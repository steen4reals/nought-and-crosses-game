import React from 'react';


function Square({value}){
   
          return (
            <button className="square" onClick={()=>  `)}>
            {value}
            </button>
          );
};

export default Square;
    
